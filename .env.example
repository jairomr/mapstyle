# Backend Environment Variables
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Frontend uses relative /api path (no env vars needed in production)
# Development: Vite proxy configured in vite.config.js
# Production: Nginx reverse proxy handles /api routes
